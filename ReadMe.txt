1. Fill the Config.json
    - Source = Pattern of the files respective to the current folder of the script
    - Destination = Final folder structure
    - RootArchiveFolder = Final name of the zip archive (Do not include .zip)

2. Script must placed in the relative path of the copying files

3. To run the script ->  .\Archive.ps1 -ConfigFile .\Config.json